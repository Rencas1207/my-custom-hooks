# useForm Hook

Ejemplo de uso:

```javascript
const initialForm = {
  name: '',
  age: 0,
  email: '',
};
const { formValues, handleInputChange, resset } = useForm(initialForm);
```

useCounter() // Recibe un valor por defecto
